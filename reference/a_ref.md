# Internal Data: Expected age per school level

`a_ref` is used within functions for specific calculations in the
package. This dataframe is not exported and is intended for internal use
only.

## Format

A data frame with elements specific to internal calculations.
